# Claude Code 프롬프트 — Lumée Maison 골드 버튼 이펙트 (디자인 시스템에서 그대로 추출)

> 아래 블록을 복사해서 Claude Code(`lumiere-app` 열린 상태)에 붙여넣으세요.
> 디자인 시스템 킷(`ui_kits/storefront/`)의 골드 버튼 hover 이펙트를 **그대로** 코드베이스에 적용합니다.
> **버튼 시각 효과만** 작업하고 기능/로직/레이아웃은 건드리지 않습니다.

---

너는 Lumée Maison(Next.js 16 · TypeScript · Tailwind v4 · framer-motion) 프론트엔드 디자이너야.
**작업 범위는 버튼 골드 hover 이펙트뿐.** 그 외(기능·라우팅·상태·텍스트·레이아웃)는 절대 수정하지 마. 새 의존성 금지.

## 적용할 이펙트 — 아래 CSS를 "정답"으로 삼아라
디자인 시스템 킷에서 그대로 가져온 값이다. `app/globals.css`의 `@layer components`에 통합하고, 값이 다르면 이 값으로 맞춰라.

```css
@layer components {
  /* 공통 베이스 */
  .btn {
    display: inline-flex; align-items: center; justify-content: center; gap: 8px;
    padding: 0.8rem 2rem;
    font-size: 0.8125rem; font-weight: 600; letter-spacing: 0.15em; text-transform: uppercase;
    border-radius: 10px; border: 1px solid transparent;
    position: relative; overflow: hidden; cursor: pointer;
    transition: all 300ms cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* === GOLD: 풀 이펙트 (시머 스윕 + 글로우 + press) === */
  .btn-gold { background: var(--gold, #c9a96e); color: #fff; border-color: var(--gold, #c9a96e); }
  .btn-gold:hover {
    background: var(--gold-dark, #a8874a); border-color: var(--gold-dark, #a8874a);
    box-shadow: 0 0 24px rgba(201, 169, 110, 0.35);
  }
  .btn-gold::after {
    content: ''; position: absolute; inset: 0; pointer-events: none;
    background: linear-gradient(120deg, transparent 30%, rgba(255,255,255,0.35) 50%, transparent 70%);
    transform: translateX(-120%);
    transition: transform 600ms ease;
  }
  .btn-gold:hover::after { transform: translateX(120%); }
  .btn-gold:active { transform: translateY(1px); box-shadow: 0 0 14px rgba(201,169,110,0.25); }
  .btn-gold:focus-visible { outline: none; box-shadow: 0 0 0 3px rgba(201,169,110,0.35); }

  /* === PRIMARY: obsidian → hover 시 골드 채움 + 글로우 === */
  .btn-primary { background: var(--obsidian, #0a0a0a); color: var(--cream, #faf8f5); }
  .btn-primary:hover {
    background: var(--gold, #c9a96e); border-color: var(--gold, #c9a96e); color: #fff;
    box-shadow: 0 6px 20px rgba(201,169,110,0.30);
  }
  .btn-primary:active { transform: translateY(1px); }
  .btn-primary:focus-visible { outline: none; box-shadow: 0 0 0 3px rgba(201,169,110,0.35); }

  /* === SECONDARY: 고스트 → hover 시 골드 보더/텍스트 + 옅은 글로우 (배경색 골드로 바꾸지 말 것) === */
  .btn-secondary { background: transparent; color: var(--obsidian, #0a0a0a); border-color: rgba(26,26,26,0.3); }
  .btn-secondary:hover {
    border-color: var(--gold-dark, #a8874a); color: var(--gold-dark, #a8874a);
    background: rgba(250,248,245,0.4); box-shadow: 0 0 22px rgba(201,169,110,0.22);
  }
  .btn-secondary:active { transform: translateY(1px); }
  .btn-secondary:focus-visible { outline: none; box-shadow: 0 0 0 3px rgba(201,169,110,0.35); }
}

@media (prefers-reduced-motion: reduce) {
  .btn, .btn-gold::after { transition: none; }
}
```

## 할 일
1. 위 CSS를 `app/globals.css`에 통합한다. (이미 `.btn-gold`가 있으면 위 스펙으로 정렬·보강.)
2. 코드베이스의 주요 CTA/액션 버튼을 찾아 알맞은 className을 붙인다:
   - 주요 행동(Add to Cart, View Catalogue, 결제/다음 단계, WhatsApp/Email CTA 등) → `btn btn-gold`
   - obsidian 채움 주요 버튼 → `btn btn-primary`
   - 보조/고스트 버튼(Contact, Back 등) → `btn btn-secondary`
   - 위치: `components/home/*`, `components/catalogue/ProductCard.tsx`, `components/product/*`, `components/checkout/*`, `components/layout/Header.tsx`
3. **핵심은 hover 골드 시머·글로우**: 골드 버튼은 시머 스윕 1회 + 글로우, 그 외는 글로우만. 한 화면에 글로우 남발 금지(주요 CTA 위주, "조용한 럭셔리").

## 성능·접근성
- `transform`/`opacity`/`box-shadow`만 애니메이트. `overflow:hidden` 유지(시머가 밖으로 안 나가게).
- 모든 버튼 `:focus-visible` 골드 링 보장.
- `prefers-reduced-motion`에서 시머·트랜지션 정지.

## 완료 기준
- [ ] 골드 버튼: 포인터 hover 시 빛 시머가 1회 매끄럽게 가로지르고 골드 글로우가 켜짐
- [ ] primary/secondary: hover 글로우 일관, 기본 배경은 의도대로 유지
- [ ] active(press) + focus-visible 상태 일관
- [ ] reduced-motion 정지, 60fps, 기능 변화 없음

---

### 참고: 비주얼 레퍼런스
- 이 효과의 살아있는 예시: 디자인 시스템의 `ui_kits/storefront/index.html`(버튼들), `preview/button-gold-effect.html`(이펙트 카드).
- 드롭인 CSS만 필요하면 `button-gold-hover.css` 사용.
